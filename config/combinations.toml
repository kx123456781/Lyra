# MOD 组合配置

# 推荐的组合（优先显示）
recommended = [1, 33, 257, 1057]

# 白名单（额外添加的组合，即使不满足规则也会生成）
whitelist = [769, 1281, 2305, 4353]

# 黑名单（排除的组合）
blacklist = []

# Polyfill 配置
[polyfill]
enabled = true
code = 1
